#!/usr/bin/env python2
# -*- coding: utf-8 -*-

""" 

:mod:`lab16_modules_packages` --- Modules and Packages
======================================================

Create new modules called module_a and module_b. In each module, add a function do_x() that returns the module name + " here"

Use *import* in module_b to import module_a and print results of calling the module_a do_x()

Create a new directory called **my_package**. Initialize **my_package** to be a package (what file is needed?). Create *module_c* in my_package that returns its module name + " here"

Use *from* statement in module_a to import the *module_c* do_x function only and rename it to do_c_x. Print the results of calling do_c_x() from *module_a*
 
Print __name__ while in *module_a* and *module_b* if != "__main__"

"""

if __name__ == "__main__":
    pass
